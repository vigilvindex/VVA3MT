/*
 * hqs_hq.sqf
 * This script checks player side and sends them to their HQ if it is available.
 */
/*
private["_caller"];
_caller = _this select 1;
if ((side _caller) == west)	then {
    if (alive w_hq) then {
        _caller setPos (getPos w_hq);
    }else{
        hint "HQ is unavailable.\n";
    };
};
if ((side _caller) == east) then {
    if (alive e_hq) then {
        _caller setPos (getPos e_hq);
    }else{
        hint "HQ is unavailable.\n";
    };
};
if ((side _caller) == resistance) then {
    if (alive g_hq) then {
        _caller setPos (getPos g_hq);
    }else{
        hint "HQ is unavailable.\n";
    };
};
if ((side _caller) == civilian) then {
    if (alive c_hq) then {
        _caller setPos (getPos c_hq);
    }else{
        hint "HQ is unavailable.\n";
    };
};
*/
